# Configuración base SkillNet.
# Desarrollo local: application.yml (PostgreSQL localhost, media local).
# Producción AWS:   spring.profiles.active=prod  →  application-prod.properties

spring.profiles.default=
